# expo-router 57.0.17 pulls in react-dom@19.2.8 transitively (web/devtools support),
# which peer-requires react@^19.2.8 while Expo SDK 57 pins react@19.2.3. The conflict
# is resolver-only and does not affect the native app, which never loads react-dom.
legacy-peer-deps=true
