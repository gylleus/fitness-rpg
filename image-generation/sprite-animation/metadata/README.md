# Local setup metadata

This directory holds generated setup diagnostics, dependency inventories and
model verification reports. It is kept in the source tree because setup writes
its supplemental requirements here before installing the upstream sources.
Generated files remain local; pinned requirements and model locks live in the
parent directory.
